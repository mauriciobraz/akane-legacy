{
  "name": "akane",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "[ -f .env ] || cp .env.example .env",
    "build": "tsc",
    "start": "node build/index.ts",
    "dev": "tsnd --respawn --transpile-only --ignore-watch build/ source/index.ts"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "tslib": "^2.4.0"
  }
}
